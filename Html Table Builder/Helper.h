#pragma once
#include<string>
#include<iostream>
#include<vector>

static class Helper
{
public:
	//Find all references (pos) into another string
	static std::vector<int> FindAllInString(std::string, const std::string&);

};

